/// Enumerated type of route jump
enum RouterType {
  material,
  cupertino,
  slide,
  scale,
  rotation,
  size,
  fade,
  scaleRotate,
}

/// Advanced usage d route enumeration enum type
enum AdvanceType {
  slide,
}
